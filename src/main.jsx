import React from 'react'
import ReactDOM from 'react-dom/client'
import { PatenteApp } from './PatenteApp'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PatenteApp />
  </React.StrictMode>
)
